version: '3'
services:

  backend:
    build: .
    restart: always
    ports:
      - "8001:8000"
    volumes:
      - .:/app

  tests:
    build: .
    command: pytest tests/ -v
    volumes:
      - .:/app
    depends_on:
      - backend